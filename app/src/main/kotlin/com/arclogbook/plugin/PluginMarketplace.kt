package com.arclogbook.plugin

object PluginMarketplace {
    fun discoverPlugins(): List<String> = listOf("OSINT Plugin", "Evidence Plugin", "Export Plugin")
    fun installPlugin(name: String) {
        // Simulate plugin installation
    }
}
